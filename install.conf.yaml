- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bash/bashrc
    ~/.bash_profile: bash/bash_profile
    ~/.vimrc: vim/vimrc_minimal
    #~/.vimrc: vim/vimrc_complete
    ~/.gvimrc: vim/gvimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
